apiVersion: move2kube.konveyor.io/v1alpha1
kind: Translator
metadata:
  name: Golang-s2i
spec:
  mode: "Container"
  class: "Executable"
  config:
    artifacts: 
      - "ContainerBuild"
    exclusiveArtifacts: 
      - "ContainerBuild"
    directoryDetectCMD: 
      cmd: "./m2kdetect.sh"