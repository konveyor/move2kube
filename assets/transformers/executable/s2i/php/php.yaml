apiVersion: move2kube.konveyor.io/v1alpha1
kind: Transformer
metadata:
  name: PHP-s2i
spec:
  mode: "Container"
  class: "SimpleExecutable"
  generates: 
    - "ContainerBuild"
    - "S2IBuilder"
  exclusive: 
    - "ContainerBuild"
    - "S2IBuilder"
  config:
    directoryDetectCMD: ["./m2kdetect.sh"]
