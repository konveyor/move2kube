apiVersion: move2kube.konveyor.io/v1alpha1
kind: Transformer
metadata:
  name: Rust
spec:
  mode: "Container"
  class: "SimpleExecutable"
  generates: 
    - "Dockerfile"
  exclusive: 
    - "Dockerfile"
  config:
    directoryDetectCMD: ["./m2kdetect.sh"]
